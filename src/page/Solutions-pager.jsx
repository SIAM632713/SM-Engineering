import React from 'react';
import Layout from "../component/Layout/layout.jsx";

const SolutionsPager = () => {
    return (
        <Layout>
            
        </Layout>
    );
};

export default SolutionsPager;